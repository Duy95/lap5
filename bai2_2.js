class rectangle {
    constructor( w , h){
        this.w = w;
        this.h = h;
    }
    perimeter() {
        return (this.w + this h) * 2 ;
    }

    area() {
        return this.w * this.h;
    }
}

let w = float(input("nhập chiều dài: "))
let h = float(input("nhập chiều rộng: "))

const rectangle1 = rectangle(this w,this h);
console.log(rectangle1.area);
console.log(rectangle1.perimeter);
